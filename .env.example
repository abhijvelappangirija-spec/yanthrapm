# Copy this file to .env.local and replace placeholder values.
# Keep real secrets only in .env.local or your deployment secret manager.

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# AI provider selection
# Supported values: perplexity, ollama, dummy
AI_DEFAULT_PROVIDER=perplexity
# AI_BRD_PROVIDER=ollama
# AI_SPRINT_PROVIDER=ollama
# AI_PRIVATE_PROVIDER=ollama
# AI_PRIVATE_TASKS=brd,brd-from-file,sprint-plan
# AI_EXTERNAL_DISABLED=false
#
# Local testing with real models: set PERPLEXITY_API_KEY and/or Ollama env vars; keep the app
# checkbox "Use dummy data only" unchecked unless you want to skip API calls for a quick UI pass.

# Controlled external retrieval
# Keep disabled unless approved-source, sanitized retrieval is required.
# When disabled, Perplexity BRD/sprint calls use disable_search (no live web) except
# an explicit pre-composition retrieval pass once you enable AI_RETRIEVAL_ENABLED.
# AI_RETRIEVAL_ENABLED=false
# AI_RETRIEVAL_ALLOW_RESTRICTED=false
# AI_RETRIEVAL_APPROVED_DOMAINS=owasp.org,nist.gov,cisa.gov,ietf.org,developer.atlassian.com,supabase.com,docs.ollama.com
# AI_RETRIEVAL_ALLOWED_USE_CASES=industry-standards,compliance-controls,technology-docs,integration-docs

# External AI provider: Perplexity
PERPLEXITY_API_KEY=your_perplexity_api_key

# Private/local AI provider: Ollama
# If AI_BRD_PROVIDER or AI_SPRINT_PROVIDER is ollama, the Next.js server must reach OLLAMA_BASE_URL
# (start Ollama before using BRD/sprint — "fetch failed" usually means nothing is listening on that URL).
# OLLAMA_BASE_URL=http://127.0.0.1:11434
# OLLAMA_MODEL=llama3.1:8b
# OLLAMA_BRD_MODEL=llama3.1:8b
# OLLAMA_SPRINT_MODEL=llama3.1:8b

# Jira Cloud
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_API_TOKEN=your_jira_api_token
# JIRA_STORY_POINTS_FIELD_ID=customfield_13805
# JIRA_EPIC_LINK_FIELD_ID=parent

# Local/dev-only actor fallback
# Leave unset in production.
# APP_ALLOW_FALLBACK_ACTOR=true
# APP_DEFAULT_USER_ID=your_local_test_user_id
# DEMO_USER_ID=your_demo_user_id
# DEFAULT_USER_ID=your_default_user_id
